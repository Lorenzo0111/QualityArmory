package me.zombie_striker.qg.hooks;

import me.zombie_striker.qg.QAMain;

public class OraxenHook {
    
    public OraxenHook(QAMain main) {
        /*main.getLogger().info("Found oraxen plugin. Merging QualityArmory resourcepack with it.");

        try {
            Field field = OraxenPlugin.get().getResourcePack().getClass().getDeclaredField("assetsFolder");
            field.setAccessible(true);
            File file = (File) field.get(OraxenPlugin.get().getResourcePack());
            if (file == null) return;


        } catch (Throwable e) {
            // Ignored
            QAMain.DEBUG("Unable to merge oraxen texture pack: " + e.getMessage());
        }*/
    }
}
